name: build
on: [pull_request, push]

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Gradle
        uses: null2264/actions/gradle-setup@1c142aac3f75e1f4564b92639f267d2b106c68bd
        with:
          java: 21

      - name: Setup NeoForge Kits for snapshots
        uses: null2264/actions/neoforge-snapshot@1c142aac3f75e1f4564b92639f267d2b106c68bd
        with:
          mc-version: 24w14a
          skip-gradle-setup: 'true'

      - name: Get mod versions
        id: get-mod-versions
        run: |
          echo "versions=$(ls versions | grep -e '.\.' | tr '\n' ' ')" >> $GITHUB_OUTPUT

      - name: Build with Gradle
        uses: null2264/actions/replaymod-preprocessor-compile@1c142aac3f75e1f4564b92639f267d2b106c68bd
        with:
          versions: ${{ steps.get-mod-versions.outputs.versions }}

      - name: Archive build results
        run: tar -I zstd -cf build.tar.zst versions/*/build/libs

      - name: Upload build folder
        uses: actions/upload-artifact@v4
        with:
          name: build-artifacts
          path: build.tar.zst
          if-no-files-found: error
          retention-days: 3
